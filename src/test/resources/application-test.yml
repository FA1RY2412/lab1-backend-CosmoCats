spring:
  jpa:
    hibernate:
      ddl-auto: create-drop

  liquibase:
    enabled: false

security:
  api-key-header: X-API-KEY
  api-key: cosmo-cats-api-key

  jwt:
    secret: cosmo-cats-jwt-secret
    jws-algorithm: HS256
    roles-claim: roles
